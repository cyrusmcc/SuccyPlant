/*

import axios from 'axios';

const axiosApi = axios.create({
    baseURL: '/api',
    timeout: 1000,
    headers: { 'Content-Type': 'application/json'}
});

interface User {
    id: number;
    firstName: String,
    lastName: String;
}

export default {
    async hello() {
        try {
            AxiosResponse = await axiosApi.get('/hello');
            return AxiosResponse;
        } catch(e) {
            console.log(e);
        }
    }
}
*/